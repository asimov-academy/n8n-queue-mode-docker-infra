# ===== N8N CORE =====
POSTGRES_USER=n8n
POSTGRES_PASSWORD=supersecret
POSTGRES_DB=n8n
REDIS_PASSWORD=redissecret
# Gere uma chave forte (32+ bytes base64):  openssl rand -base64 32
N8N_ENCRYPTION_KEY=coloque_sua_chave_ssh_em_base64_aqui

# ===== AI AGENT: POSTGRES (database-tool) =====
TOOL_DB_USER=agent
TOOL_DB_PASSWORD=agentpass
TOOL_DB_NAME=agentdb

# ===== AI AGENT: REDIS (memory) =====
MEMORY_PASSWORD=memorysecret

# ===== PGADMIN (UI para database-tool) =====
PGADMIN_DEFAULT_EMAIL=admin@local
PGADMIN_DEFAULT_PASSWORD=pgadminpass
